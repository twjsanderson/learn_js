/**
 * setup node
 * run in browser
 * run in free applications on line
 * run in node
 * run files from node
 * 
 * 
 * We are going to want to run our javascript files locally (on our own machine) and 
 * the best way to do that is using Node.js. This run time environment is great 
 * for running javascript outside of the browser and is pretty much a critical 
 * piece of software to have installed on your machine if you want to build modern
 * JS applications.
 * 
 * We could just download the binary depending on our OS but a more efficient way of
 * managing different versions of Node.js (keeping things current) is to use a package
 * manager. I choose to use a really popular one called NVM (Node version manager). 
 * 
 * instead of this https://nodejs.org/en/download/
 * 
 * https://nodejs.org/en/download/package-manager/
 * 
 * 
 */